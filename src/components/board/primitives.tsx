/**
 * Moved to components/ui/primitives — these are the product's shared
 * primitives now, not board-only ones. Kept as a re-export so every existing
 * `./primitives` import inside components/board keeps working unchanged.
 */
export * from "../ui/primitives";
